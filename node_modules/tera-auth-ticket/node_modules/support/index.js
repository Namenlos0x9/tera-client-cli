/**
 * note - this is an incomplete library of support's most common libs.
 */

module.exports = {
    gate: require('./gate'),
    Gate: require('./gate'),
    nakamura_gate: require('gate'),

    proper_path: require('./proper_path'),

    pipe: require('./pipe')

}